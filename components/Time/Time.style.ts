import css from 'styled-jsx/css';
import * as vars from '@styles/vars';

export default css`
  .wrapper {
    font-size: 1.3rem;
    color: ${vars.grey3};
    font-weight: 300;
    position: relative;
  }
`;
